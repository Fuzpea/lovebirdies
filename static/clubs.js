let ct=localStorage.getItem('lb_club_token')||'';const $=s=>document.querySelector(s);function toast(s){$('#toast').textContent=s;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2200)}async function api(p,o={}){o.headers={'Content-Type':'application/json',...(ct?{Authorization:`Bearer ${ct}`}:{})};let r=await fetch(p,o),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.detail||'Request failed');return d}function body(f){return Object.fromEntries(new FormData(f).entries())}async function boot(){if(!ct)return;try{let c=await api('/api/clubs/me');$('#clubAuth').classList.add('hidden');$('#clubDash').classList.remove('hidden');$('#clubName').textContent=c.name;$('#clubMeta').textContent=c.location+(c.verified?' · Verified club':' · Club profile');loadEvents()}catch{ct='';localStorage.removeItem('lb_club_token')}}$('#clubRegister').onsubmit=async e=>{e.preventDefault();try{let d=body(e.target);let r=await api('/api/clubs/register',{method:'POST',body:JSON.stringify(d)});ct=r.token;localStorage.setItem('lb_club_token',ct);boot();toast('Club profile created')}catch(x){toast(x.message)}};$('#clubLogin').onsubmit=async e=>{e.preventDefault();try{let r=await api('/api/clubs/login',{method:'POST',body:JSON.stringify(body(e.target))});ct=r.token;localStorage.setItem('lb_club_token',ct);boot()}catch(x){toast(x.message)}};$('#clubLogout').onclick=()=>{ct='';localStorage.removeItem('lb_club_token');location.reload()};$('#eventForm').onsubmit=async e=>{e.preventDefault();try{let d=body(e.target);d.price=Number(d.price);d.capacity=Number(d.capacity);await api('/api/clubs/events',{method:'POST',body:JSON.stringify(d)});toast('Singles Golf Night published');loadEvents()}catch(x){toast(x.message)}};async function loadEvents(){let a=await api('/api/clubs/events');$('#clubEvents').innerHTML=a.length?a.map(e=>`<article class="golf-card"><div class="golf-card-icon">🎉</div><div><div class="eyebrow">${e.status}</div><h3>${esc(e.title)}</h3><p><b>${date(e.event_date)}</b> · ${e.event_time} · ${esc(e.format)}</p><p>${e.booked}/${e.capacity} booked · €${Number(e.price).toFixed(2)}</p>${e.status==='published'?`<button class="linkbtn cancel-event" data-id="${e.id}">Cancel event</button>`:''}</div></article>`).join(''):'<p class="meta">No events yet. Create your first Singles Golf Night.</p>';document.querySelectorAll('.cancel-event').forEach(b=>b.onclick=async()=>{if(confirm('Cancel this event?')){await api('/api/clubs/events/'+b.dataset.id+'/cancel',{method:'PUT'});loadEvents()}})}function date(s){return new Date(s+'T12:00:00').toLocaleDateString([],{weekday:'short',day:'numeric',month:'short',year:'numeric'})}function esc(s){return String(s||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}boot();