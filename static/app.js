let token=localStorage.getItem('lb_token')||'';let me=null;let currentChat=null;const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);
async function api(path,opt={}){opt.headers={...(opt.headers||{}),'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})};let r=await fetch(path,opt);let data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.detail||'Something went wrong');return data}
function toast(s){$('#toast').textContent=s;$('#toast').classList.add('show');setTimeout(()=>$('#toast').classList.remove('show'),2300)}
function authUI(on){$('#welcome').classList.toggle('hidden',on);$('#app').classList.toggle('hidden',!on);$('#nav').style.display=on?'flex':'none'}
async function boot(){if(!token)return authUI(false);try{me=await api('/api/me');authUI(true);showView('discover');await loadDiscover()}catch(e){localStorage.removeItem('lb_token');token='';authUI(false)}}
function openAuth(mode){$('#authDialog').showModal();$('#authForm').dataset.mode=mode;let reg=mode==='register';$('#registerFields').classList.toggle('hidden',!reg);$('#authTitle').textContent=reg?'Create account':'Welcome back';$('#authSub').textContent=reg?'Start with the basics. You can complete your profile next.':'Log in to continue discovering golfers.';$('#authSubmit').textContent=reg?'Create account':'Log in';$('#authError').textContent=''}
$('#showRegister').onclick=()=>openAuth('register');$('#showLogin').onclick=()=>openAuth('login');$('#closeAuth').onclick=()=>$('#authDialog').close();
$('#authForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target),mode=e.target.dataset.mode;let body={email:f.get('email'),password:f.get('password')};if(mode==='register')Object.assign(body,{name:f.get('name'),age:Number(f.get('age')),location:f.get('location'),handicap:f.get('handicap')?Number(f.get('handicap')):null});try{let d=await api('/api/'+mode,{method:'POST',body:JSON.stringify(body)});token=d.token;localStorage.setItem('lb_token',token);$('#authDialog').close();await boot();toast(mode==='register'?'Welcome to LoveBirdies':'Welcome back')}catch(err){$('#authError').textContent=err.message}};
$('#logout').onclick=()=>{token='';me=null;localStorage.removeItem('lb_token');authUI(false)};
function showView(v){$$('.view').forEach(x=>x.classList.add('hidden'));$('#'+v+'View').classList.remove('hidden');$$('.side').forEach(x=>x.classList.toggle('active',x.dataset.view===v));if(v==='matches')loadMatches();if(v==='profile')loadProfile()}
$$('[data-view]').forEach(b=>b.onclick=()=>showView(b.dataset.view));$('#refresh').onclick=loadDiscover;
function card(u){let photo=u.photo||'https://images.unsplash.com/photo-1535131749006-b7f58c99034b?auto=format&fit=crop&w=900&q=80';return `<article class="profile" data-id="${u.id}"><div class="profile-photo" style="background-image:url('${photo}')"></div><div class="profile-body"><h3>${esc(u.name)}, ${u.age} ${u.verified?'✓':''}</h3><div class="meta">${esc(u.location)} · ${u.handicap??'—'} handicap</div><p class="bio">${esc(u.bio||'New to LoveBirdies and ready to meet fellow golfers.')}</p><div class="meta">${esc(u.golf_style||'Golfer')}${u.home_club?' · '+esc(u.home_club):''}</div><div class="actions"><button class="circle pass" title="Pass">×</button><button class="circle like" title="Like">♥</button></div></div></article>`}
async function loadDiscover(){let users=await api('/api/discover');$('#profiles').innerHTML=users.length?users.map(card).join(''):'<p>No new profiles right now. Try refreshing later.</p>';$$('.profile .pass').forEach(b=>b.onclick=()=>b.closest('.profile').remove());$$('.profile .like').forEach(b=>b.onclick=async()=>{let el=b.closest('.profile'),id=Number(el.dataset.id);let d=await api('/api/like/'+id,{method:'POST'});el.remove();if(d.matched)$('#matchDialog').showModal();else toast('Liked — we’ll show you if it becomes a match.')})}
$('#closeMatch').onclick=()=>$('#matchDialog').close();
async function loadMatches(){let users=await api('/api/matches');$('#matches').innerHTML=users.length?users.map(u=>`<div class="match-row"><div class="avatar" style="background-image:url('${u.photo||''}')"></div><div class="grow"><h3>${esc(u.name)}, ${u.age}</h3><div class="meta">${esc(u.location)} · ${u.handicap??'—'} handicap</div></div><button class="primary msgbtn" data-id="${u.id}" data-name="${esc(u.name)}">Message</button></div>`).join(''):`<div><p>You don't have a mutual match yet.</p><p class="meta">For testing, like Aoife then use the demo-match helper from your browser console if needed: <code>demoMatch(1)</code>.</p></div>`;$$('.msgbtn').forEach(b=>b.onclick=()=>openChat(Number(b.dataset.id),b.dataset.name))}
async function loadProfile(){me=await api('/api/me');let f=$('#profileForm');for(let [k,v] of Object.entries(me)){if(f.elements[k])f.elements[k].value=v??''}}
$('#profileForm').onsubmit=async e=>{e.preventDefault();let f=new FormData(e.target),body={};for(let [k,v] of f.entries())body[k]=['age','handicap'].includes(k)&&v!==''?Number(v):v;await api('/api/me',{method:'PUT',body:JSON.stringify(body)});toast('Profile saved')};
async function openChat(id,name){currentChat=id;showView('chat');$('#chatHeader').innerHTML=`<div class="eyebrow">Private chat</div><h2>${esc(name)}</h2>`;await refreshMessages()}
async function refreshMessages(){if(!currentChat)return;let ms=await api('/api/messages/'+currentChat);$('#messages').innerHTML=ms.map(m=>`<div class="msg ${m.sender_id===me.id?'mine':''}">${esc(m.body)}<small>${new Date(m.created_at+'Z').toLocaleString()}</small></div>`).join('');$('#messages').scrollTop=$('#messages').scrollHeight}
$('#messageForm').onsubmit=async e=>{e.preventDefault();let input=$('#messageInput'),body=input.value.trim();if(!body)return;await api('/api/messages',{method:'POST',body:JSON.stringify({receiver_id:currentChat,body})});input.value='';await refreshMessages()};
window.demoMatch=async id=>{let d=await api('/api/demo-match/'+id,{method:'POST'});toast('Demo match created');return d};
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
boot();
